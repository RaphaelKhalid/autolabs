import type { Metadata } from 'next';
import Link from 'next/link';
import { erdosPilot as pilot, pilotSnapshot as snapshot, pilotReportUrl } from '@/lib/experiment-catalog';
import { factorizationFromDifference } from '@/lib/exact-verifier';
import { ArchivedLedger } from '@/components/archived-ledger';

export const metadata: Metadata = { title: 'Erdős 885: the 100-round pilot · AutoLabs', description: 'Completed pilot: 100 rounds, 606 computation jobs, no certified k=5 solution or frontier improvement. Explore the evidence and lessons.' };
export default function PilotPage() {
  const candidate = snapshot.candidate;
  const cells = candidate.numbers.map((n) => candidate.differences.map((d) => factorizationFromDifference(BigInt(n), BigInt(d))));
  return <main className="archive-page">
    <nav className="archive-nav" aria-label="Primary"><Link href="/">A / AUTOLABS</Link><Link href="/experiments">All experiments</Link></nav>
    <header className="archive-heading"><p className="archive-kicker">PILOT 001 / COMPLETE / 3–8 SEPTEMBER 2026</p><h1>100 rounds.<br/><em>An open record.</em></h1><p>Five Luna High researchers investigated Erdős problem 885 for k = 5. This was one pilot experiment with 100 synchronized rounds—not 100 independent trials.</p></header>
    <section className="archive-verdict" aria-label="Pilot outcome"><h2>No certified k = 5 solution.<br/>No verified frontier improvement.</h2><p>The final report confirms completion, not a pause. The retained candidates are computational evidence, not an established new theorem. Field-level novelty has not been established.</p><a href={pilotReportUrl}>Inspect the dated terminal report ↗</a></section>
    <div className="archive-stats"><div><strong>100</strong><span>rounds completed</span></div><div><strong>606</strong><span>computation jobs</span></div><div><strong>87</strong><span>candidate-check records</span></div><div><strong>2,611</strong><span>ledger events</span></div></div>
    <section className="archive-section"><p className="archive-kicker">01 / THE ACTUAL RESULT</p><h2>What the best score represents</h2><p>The final internal leader was submitted by {candidate.agent} in round {candidate.round}: 21 valid cells in a 7-row, 6-column table. It is <strong>not a complete 7 × 6 rectangle</strong>, nor “21 out of 25” toward a solution. A k = 5 witness needs a complete five-by-five subtable.</p>
      <div className="candidate-scroll" role="region" aria-label="Exact candidate table" tabIndex={0}><table className="candidate-table"><caption>Each filled cell gives exact factors a × b = N with b − a = d. A dash means no such positive-integer factor pair.</caption><thead><tr><th scope="col">N / d</th>{candidate.differences.map(d => <th scope="col" key={d}>{d}</th>)}</tr></thead><tbody>{candidate.numbers.map((n, row) => <tr key={n}><th scope="row">{n}</th>{cells[row].map((cell, col) => <td key={candidate.differences[col]} className={cell ? 'is-exact' : ''}>{cell ? `${cell.factorPair.a} × ${cell.factorPair.b}` : '—'}</td>)}</tr>)}</tbody></table></div>
      <p className="archive-caption">This is the run’s final heuristic leader, not a claim to the closest candidate in the literature. “Novel” in an agent-authored label is not independent evidence of novelty.</p>
    </section>
    <section className="archive-section"><p className="archive-kicker">02 / EXPERIMENT CONFIGURATION</p><h2>The setup belongs to the experiment.</h2><dl className="archive-config"><div><dt>Question</dt><dd>{pilot.objective}</dd></div><div><dt>Researchers</dt><dd>{pilot.researchers} × {pilot.model}, reasoning effort {pilot.reasoning}</dd></div><div><dt>Cadence</dt><dd>{pilot.protocol} Pauses, retries and infrastructure recovery extended elapsed time.</dd></div><div><dt>Tools</dt><dd>Exa retrieval; bounded family scans, boundary scans and divisor completion; exact integer verification.</dd></div><div><dt>Acceptance</dt><dd>{pilot.verifier} Mathematical novelty requires a separate literature assessment.</dd></div><div><dt>Interventions</dt><dd>Round-25 diversity review; round-50 workflow council; approved workflow changes from round 56. Human-directed policy changes and operational repairs were part of this pilot.</dd></div></dl></section>
    <section className="archive-section"><p className="archive-kicker">03 / COST & RELIABILITY</p><h2>Budget is not the same as spend.</h2><div className="candidate-scroll"><table className="candidate-table"><caption>Recorded estimates in USD, audited 8 September 2026</caption><thead><tr><th>Provider</th><th>Pilot only</th><th>Including earlier runs</th><th>Allocated ceiling</th></tr></thead><tbody><tr><th>OpenAI</th><td>${snapshot.openaiUsd.toFixed(2)}</td><td>${snapshot.cumulativeOpenaiUsd.toFixed(2)}</td><td>${pilot.budgets.openai}</td></tr><tr><th>Exa</th><td>${snapshot.exaUsd.toFixed(2)}</td><td>${snapshot.cumulativeExaUsd.toFixed(2)}</td><td>${pilot.budgets.exa}</td></tr></tbody></table></div><p className="archive-caption">Application-ledger estimates, not reconciled provider bills. Unreturned usage on failed or timed-out requests may be missing. Hosting, Codex subscription costs, human time and separately funded agent rewards are excluded.</p><p>297 computation jobs were marked complete, 297 partial, and 12 failed. “Complete” describes an executed job’s bounded domain; it does not establish a theorem, global exhaustion, or that every proposed constraint was implemented.</p></section>
    <section className="archive-section"><p className="archive-kicker">04 / LESSONS FROM THE PILOT</p><h2>What we would carry forward</h2><ol className="archive-lessons"><li><h3>Check the metric before optimizing it.</h3><p>Row-only support could favor the wrong shapes. A balanced row-and-column score was introduced, but an improving heuristic still is not a proof.</p></li><li><h3>Give diversity an execution path.</h3><p>Different personas did not prevent convergence on divisor-based searches. Method rotation and explicit stop rules mattered; an idea outside the available calculator tools remained just an idea.</p></li><li><h3>Verify the executed search, not its description.</h3><p>Exact arithmetic can validate a witness. An exclusion claim also needs the actual domain, enforced constraints and completeness information. Late-run certificate repairs underline that distinction.</p></li><li><h3>Make autonomy recoverable.</h3><p>Durable jobs and retained records allowed recovery from API timeouts, infrastructure limits and a final-report size failure. This was supervised autonomy, not an untouched overnight run.</p></li></ol><p className="archive-caption">Observations from one evolving pilot. Without control runs, they do not establish that five agents beat one, or that a particular model or collaboration protocol is superior.</p></section>
    <section className="archive-section" id="record"><p className="archive-kicker">05 / THE PILOT LEDGER</p><h2>Evidence, with its limits visible.</h2><p>The terminal report contains candidate checks, {snapshot.privatePlans} released private plans, {snapshot.retrievals} retrieval records and a computation-job index. Source anchors and failed avenues are excerpted; raw job inputs and outputs are not bundled into that compact report. The event ledger below is paginated and pinned to this pilot, independent of future experiments.</p><a className="archive-button" href={pilotReportUrl}>Open report JSON ↗</a><ArchivedLedger /></section>
    <footer className="archive-section"><Link href="/">Return to the animated lab →</Link><p className="archive-caption">Run {pilot.runId} · Report finalized {snapshot.completedAt}</p></footer>
  </main>;
}
