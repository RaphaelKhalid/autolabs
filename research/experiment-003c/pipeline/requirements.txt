# torch is assumed preinstalled on the RunPod image (2.4+, CUDA 12.8).
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
numpy>=1.26
datasets>=2.20
huggingface_hub>=0.24
requests>=2.32
# No new pip dependency: describe.py's description-clustering embeddings
# (see README "Describe stage") load sentence-transformers/all-MiniLM-L6-v2
# through the transformers/huggingface_hub already listed above (plain
# AutoTokenizer/AutoModel, no sentence-transformers package), downloaded
# lazily (~90MB) and cached under HF_HOME on first use.
zstandard>=0.22  # monology/pile-uncopyrighted ships .jsonl.zst shards
