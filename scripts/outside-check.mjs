const tabs=await(await fetch('http://127.0.0.1:9333/json/list')).json();const t=tabs.find(x=>x.type==='page');const w=new WebSocket(t.webSocketDebuggerUrl);let i=1;const p=new Map();w.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}};await new Promise((a,b)=>{w.onopen=a;w.onerror=b});const c=(method,params={})=>new Promise(a=>{const id=i++;p.set(id,a);w.send(JSON.stringify({id,method,params}))});const e=async x=>(await c('Runtime.evaluate',{expression:x,returnByValue:true,awaitPromise:true})).result?.result?.value;await e(`(()=>{const b=document.querySelector('.site-header__dropdown-trigger');if(b.getAttribute('aria-expanded')!=='true')b.click();return b.getAttribute('aria-expanded')})()`);await c('Input.dispatchMouseEvent',{type:'mousePressed',x:4,y:300,button:'left',clickCount:1});await c('Input.dispatchMouseEvent',{type:'mouseReleased',x:4,y:300,button:'left',clickCount:1});await new Promise(r=>setTimeout(r,150));console.log(JSON.stringify(await e(`(()=>({expanded:document.querySelector('.site-header__dropdown-trigger')?.getAttribute('aria-expanded'),menu:!!document.querySelector('.site-header__dropdown-menu')}))()`)));w.close();
